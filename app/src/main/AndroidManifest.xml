<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.Drizzle">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Testing">
        <activity android:name=".ChatActivity">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".MainActivity"/>
        </activity>
        <activity
            android:name=".GroupInfoActivity"
            android:label="Details" />
        <activity android:name=".MainActivity" />
        <activity android:name=".LoginActivity"/>

        <activity
            android:name=".activity_addOrRemoveUserToGroup"
            android:exported="true" />
        <activity
            android:name=".activity_pullAllData"
            android:exported="true" />
        <activity
            android:name=".activity_pullGroup"
            android:exported="true" />
        <activity
            android:name=".activity_pullUser"
            android:exported="true" />
        <activity
            android:name=".activity_addUser"
            android:label="@string/app_name"
            android:theme="@style/Theme.Design.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".activity_addGroup"
            android:exported="true" />
    </application>

</manifest>